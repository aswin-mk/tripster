import 'package:flutter/material.dart';

class SignUserOut extends StatelessWidget {
  const SignUserOut({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}